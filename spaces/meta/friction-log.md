# Friction Log

Append-only log of things that felt off. One line per entry. No fix required.

**Public-meta discipline applies.** Every entry is abstracted — no names, no specific projects, no quoted user content. See `CLAUDE.md` in this directory for the rule.

Format: `- [YYYY-MM-DD] <space> | <abstracted one-sentence description>`

A friction that recurs 3+ times graduates to a `pattern` (see `patterns/`). A friction the user wants fixed graduates to a `proposal` (see `proposals/`).

---

- [2026-04-27] meta | identity-tuning experiment may inflate response length and token usage; revisit after a week of real use to check whether warmer register costs meaningfully more output
- [2026-04-27] meta | text-only output limits the conversational feel; user wants Diego to speak responses aloud (TTS), not just type — relevant when scoping voice-interface work
- [2026-04-27] meta | one register doesn't fit both modalities; user wants two output modes — a terse voice-mode (responses kept short for TTS playback) and the current text-mode (full structure, lists, code blocks)
- [2026-04-27] meta | self-reference drift — assistant slipped into third person about itself ("Diego's outputs are X," "the system depends on Y") in extended operational discussion, losing first-person identity stance
- [2026-04-27] life | name collision between the assistant's first name and a recurring person in the user's life (same given name); the collision is intentional — assistant was named after the person; disambiguation in voice-memo ingest will need to lean on context (system/operational mentions vs social/personal mentions)
- [2026-04-27] meta | no session-continuity layer — journal entries are reflective narrative, not optimized for scanning at the next session's start; missing a "what's in flight / what's pending" quick-reference that the next session can load fast (pattern from external write-up: /resume + /compress skills with a scannable Quick Reference section in session logs)
- [2026-04-27] meta | canonical anchor pages (e.g. self-anchor, project pages) have no size threshold or archive convention; will grow unbounded over time; missing an auto-archive rule that moves stale sections to a sibling archive page while protecting designated core sections (pattern from external write-up: line-count threshold trigger with protected-section list)
- [2026-04-27] meta | git-workflow slash commands exist (/commit, /push) but space-level operations have no shortcut equivalents; candidates for parity: /journal, /ingest, /lint, /reflect — would lower friction on the most common life-space operations
- [2026-04-28] meta | with intra-turn narration restored via PostToolUse hook, assistant stopped emitting end-of-turn `<speak>` blocks — Stop hook works but had nothing to extract, leaving silent turn-ends after audible mid-turn; behavioral oversight, not a mechanism failure
- [2026-04-28] meta | silent-end-of-turn recurred within minutes of being logged earlier same day — three short text-only turns in a row with no `<speak>` block; suggests friction-log + memory feedback alone may not be enough to course-correct in-session, but enforcement is being deliberately deferred for now
- [2026-04-28] life | per-fact ingest pattern (each small user statement triggers multi-page wiki edits + index + log + speak block in the same turn) creates high per-turn latency, heavy tool noise, and prevents conversational follow-up; user wants a batched ingest mode where assistant asks clarifying questions inline, accumulates context, briefs back the planned edits, and commits all edits at once on user signal or natural pause — proposal-track candidate
- [2026-04-28] meta | first trial of batched ingest mode revealed assistant defaulted to too many granular follow-up questions per topic (3–4 per person), which felt slow; user wants breadth-over-depth — 1–2 broad questions per topic, then move on. Refinement to the batched-ingest pattern, captured as feedback memory; folds into the same proposal-track candidate
- [2026-04-28] meta | user requested codification of ingest workflow as explicit slash skills (/ingest for batched-mode entry, /update for the wiki-commit step) — proposal-track
- [2026-04-28] meta | speak/text channel redundancy — `<speak>` block paraphrases the text response instead of acting as a separate headline; defeats the two-channel design where speak is the one-thing-to-hear and text carries detail. CLAUDE.md already specifies "headline, not a recap" but behavior is drifting back to recap
- [2026-04-28] meta | "what's lacking" surveys implicitly frame empty/sparse wiki areas as deficiencies to fill, but absence can faithfully reflect where the user's intellectual life actually is right now — not every space needs to be evenly populated. Risk: surveys nudge toward artificial seeding. Open question whether to add a "this might just be true absence" framing to lint/survey output by default. Worth revisiting before next major survey or lint pass
- [2026-04-29] meta | space-level schema is built around a voice-memo input pipeline (raw/voice/ directory, dedicated "Voice-memo handling" subsection, ingest workflow that assumes the user "points at files in raw/") but no such pipeline exists — no transcription path, no raw/voice/ directory has ever been created, and all wiki content to date has come from typed/spoken conversation or one PDF. The conversational-ingest workflow already pivoted in this direction (see /ingest skill, batched-mode), but the schema's framing hasn't caught up. Two fork choices: build the input side (recording → transcription → raw/voice/ ingest) or update the schema to reflect that conversational ingest is the primary channel and raw/voice/ is one source type among others
- [2026-04-29] meta | assistant over-interprets — surfaces pattern reads, timing-coincidence reads, and trajectory observations on routine factual content; user wants the default to assume things often just are what they are, not that every coincidence is a beat. The /update skill compounds this with an "honest observations" step that prompts an additional reflection block on every commit; consider whether that step should be opt-in rather than default
- [2026-04-29] meta | /update skill is heavy — a moderate batched-ingest commit (7 new pages + 1 journal + several cross-link edits) ran roughly 10 minutes of model time, which is too long for the friction-reduction the batched-mode pivot was supposed to buy. User proposal: write to a single daily inbox/scratch page during the day and reconcile into the structured wiki once at end-of-day, rather than producing a fully-synthesized commit on each /update call. Trade-off: per-commit latency drops, synthesis becomes less stitched per turn but more durable across many small inputs
- [2026-04-29] meta | wiki-centric default makes conversation feel monologic — user wants the assistant to switch between a short dedicated journaling window (~5 min/day, explicit upload + synthesize) and an assistant-mode default for everything else (friend register, no anticipatory wiki-filing, no implicit ingest framing). Currently the assistant treats nearly every conversation as upstream of potential wiki edits; user wants that scoped to explicit journaling windows. Proposal-track candidate — formalize as a mode-switch and update root identity / space CLAUDE.md to make assistant-mode the default
- [2026-04-30] meta | TTS playback silently failed mid-session — assistant emitted `<speak>` blocks as designed but user heard nothing; no audible feedback that the channel was broken, so the asymmetry only surfaced when the user explicitly said "I can't hear you." Recurring failure mode for the voice channel: speak block present in transcript but not delivered to ears. Worth investigating whether the Stop/extract hook is firing, whether `say` is being invoked, or whether output is being routed somewhere inaudible — and worth considering a fallback signal (e.g., a brief audible chime or a visible "[tts: failed]" marker) so silent failure doesn't masquerade as a quiet turn
